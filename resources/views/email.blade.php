<!doctype html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <div class="content">
        <div class="content-inner">
            {!! $html !!}
        </div>
    </div>
</html>
